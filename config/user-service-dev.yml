spring:
  application:
    name: order-service
    
server:
  port: 8201

eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka,http://eureka2:2002/eureka    
     
feign:
  htstrix:
    enabled: true
    
management: 
  endpoints:
    web:
      exposure:
        include: htstrix.stream
        
       
              